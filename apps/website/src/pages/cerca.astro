---
/**
 * `/cerca/` — the site's one search destination.
 *
 * A route declaration, nothing more. The page is `views/search/SearchPage`.
 */
import SearchPage from '~/views/search/SearchPage.astro';

export const prerender = false;
---

<SearchPage />
