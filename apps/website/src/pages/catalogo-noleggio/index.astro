---
/**
 * `/catalogo-noleggio/` — products offered for rent.
 *
 * The URL predates the three-surface split and always meant this listing, so it
 * keeps it: renaming it would be an SEO event for no gain (CLAUDE.md § routes).
 *
 * A route declaration, nothing more. The page is `views/catalog/CatalogPage`.
 */
import CatalogPage from '~/views/catalog/CatalogPage.astro';

export const prerender = false;
---

<CatalogPage view="rental" />
