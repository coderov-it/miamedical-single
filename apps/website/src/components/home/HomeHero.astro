---
/**
 * Home hero — the owner's reference, miamedicalitalia.it (2026-08-31): a white
 * band carrying the copy on the left and the showcase card on the right, then
 * a pale band that the booking card straddles, with the trust strip on it.
 *
 * The copy column reads: location pill → two-line headline → one paragraph →
 * the two CTAs ("rent now" + WhatsApp) → three green-check assurances. This
 * supersedes the 2026-08-27 no-button rule: the reference puts the primary
 * action back in the copy, and the booking card below keeps the search.
 *
 * The white band's bottom padding and the pale band's negative margin are
 * one number seen twice — that overlap is the whole composition, so change
 * them together.
 */
import HomeBooking from '~/components/home/HomeBooking.astro';
import HomeShowcase from '~/components/home/HomeShowcase.astro';
import HomeTrustBar from '~/components/home/HomeTrustBar.astro';
import type { ProductSummary } from '~/lib/catalog';
import { localeForRequest, translate } from '~/lib/i18n';
import { routePath } from '~/lib/routes';
import { whatsappUrl } from '~/lib/site';
import { EDITORIAL_CONTAINER, EDITORIAL_THEME } from '~/styles/editorial';

interface Props {
  products: ProductSummary[];
}

const { products } = Astro.props;
const locale = localeForRequest();
const text = (key: string) => translate(locale, key);

const checks = [
  text('home.hero.check.pay'),
  text('home.hero.check.sanitized'),
  text('home.hero.check.pickup'),
];
---

<section
  class:list={[EDITORIAL_THEME, '[--hero-overlap:clamp(64px,calc(3.57vw+38.3px),84px)]']}
  aria-labelledby="home-title"
>
  <div class="bg-white pt-[clamp(36px,calc(3.57vw+10.3px),56px)] pb-[calc(var(--hero-overlap)+18px)]">
    <div
      class:list={[
        EDITORIAL_CONTAINER,
        'wide:grid-cols-[minmax(0,1.6fr)_minmax(360px,1fr)] wide:gap-x-14 grid items-center gap-10',
      ]}
    >
      <div>
        <p
          class="inline-flex items-center gap-2 rounded-full bg-accent-tint px-4 py-2 text-[0.9375rem] leading-[1.35] font-bold text-[var(--color-accent)]"
        >
          <svg
            class="flex-none"
            width="17"
            height="17"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.9"
            stroke-linecap="round"
            stroke-linejoin="round"
            aria-hidden="true"
          >
            <path d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z"></path>
            <path d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z"
            ></path>
          </svg>
          {text('home.hero.kicker')}
        </p>

        <h1
          class="mt-5 text-[clamp(2.125rem,3.3vw,2.625rem)] leading-[1.14] font-[760] tracking-[-0.028em]"
          id="home-title"
        >
          {text('home.hero.line1')}<br />
          {text('home.hero.line2')}
        </h1>

        <p class="mt-5 max-w-[52ch] text-[1.0625rem] leading-[1.65] text-[var(--color-ink-soft)]">
          {text('home.hero.sub')}
        </p>

        <div class="mt-7 flex flex-wrap items-center gap-3">
          <a
            class="inline-flex min-h-13.5 items-center justify-center gap-2.5 rounded-[var(--radius-control)] bg-[var(--color-accent)] px-7 text-[1.0625rem] font-semibold text-white no-underline hover:bg-[var(--color-accent-deep)]"
            href={routePath(locale, 'catalogRental')}
          >
            {text('home.hero.rentNow')}
            <svg
              width="18"
              height="18"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <path d="M4.5 12h15m0 0-6-6m6 6-6 6"></path>
            </svg>
          </a>
          <a
            class="inline-flex min-h-13.5 items-center justify-center gap-2.5 rounded-[var(--radius-control)] bg-[var(--color-phone-tint)] px-6 text-[1.0625rem] font-semibold text-[var(--color-phone-ink)] no-underline hover:bg-[#d3ecdd]"
            href={whatsappUrl()}
            target="_blank"
            rel="noopener"
            data-analytics="whatsapp"
          >
            <svg class="flex-none" width="19" height="19" viewBox="0 0 24 24" aria-hidden="true">
              <path
                fill="currentColor"
                d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413Z"
              ></path>
            </svg>
            {text('home.hero.whatsapp')}
          </a>
        </div>

        <ul
          class="mt-6 flex list-none flex-wrap gap-x-5 gap-y-3 [&_li]:flex [&_li]:items-center [&_li]:gap-2 [&_li]:text-sm [&_li]:font-semibold [&_svg]:flex-none [&_svg]:text-[var(--color-live)]"
        >
          {
            checks.map((check) => (
              <li>
                <svg
                  width="19"
                  height="19"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  aria-hidden="true"
                >
                  <circle cx="12" cy="12" r="9.25" />
                  <path d="m8.5 12.2 2.4 2.4 4.6-4.9" />
                </svg>
                {check}
              </li>
            ))
          }
        </ul>
      </div>

      <HomeShowcase products={products} />
    </div>
  </div>

  {
    /*
      `flow-root` matters: without it the container's negative margin collapses
      through this wrapper and drags the pale background up over the card. A
      new formatting context blocks the collapse without clipping the overlap.
    */
  }
  <div class="flow-root bg-[var(--color-page)]">
    <div class:list={[EDITORIAL_CONTAINER, 'relative -mt-[var(--hero-overlap)] pb-9']}>
      <HomeBooking />
      <HomeTrustBar />
    </div>
  </div>
</section>
