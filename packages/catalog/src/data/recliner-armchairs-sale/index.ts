/**
 * Vendita Poltrone Reclinabili, assembled — one product.
 */

import { reclinerArmchairsSale } from './category.ts';
import { ladySlimPlus } from './lady-slim-plus.ts';

export default reclinerArmchairsSale.withProducts([
  ladySlimPlus,
]);
