/**
 * Vendita elettromedicali, assembled — with no products, which is what the
 * live site has. See `category.ts`.
 */
import { electromedicalSale } from './category.ts';

export default electromedicalSale.withProducts([]);
